@model LuxRentals.ViewModels.Cars.Admin.VehicleClassIndexVm

@{
    ViewData["Title"] = "Manage Vehicle Classes";
}

<section class="py-4">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
        <div>
            <h1 class="mb-1">Manage Vehicle Classes</h1>
            <p class="text-muted mb-0">Add class options used by the car inventory form.</p>
        </div>
        @if (!string.IsNullOrWhiteSpace(Model.ReturnUrl))
        {
            <a class="btn btn-outline-secondary" href="@Model.ReturnUrl">Back to Car</a>
        }
        else
        {
            <a class="btn btn-outline-secondary" asp-area="Admin" asp-controller="Cars" asp-action="Index">Back to Inventory</a>
        }
    </div>

    @if (TempData["StatusMessage"] is string statusMessage)
    {
        <div class="alert alert-success">@statusMessage</div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="alert alert-danger">@errorMessage</div>
    }

    <div class="row g-4">
        <div class="col-lg-5">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h2 class="h5 mb-3">Add Vehicle Class</h2>

                    <form method="post" asp-area="Admin" asp-controller="VehicleClasses" asp-action="Create">
                        <input asp-for="ReturnUrl" type="hidden" />

                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="VehicleClassName" class="form-label"></label>
                            <input asp-for="VehicleClassName" class="form-control" />
                            <span asp-validation-for="VehicleClassName" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-primary">Add Vehicle Class</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-7">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h2 class="h5 mb-3">Current Vehicle Classes</h2>

                    @if (Model.VehicleClasses.Count == 0)
                    {
                        <p class="text-muted mb-0">No vehicle classes found.</p>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-sm align-middle mb-0">
                                <thead>
                                <tr>
                                    <th>Vehicle Class</th>
                                    <th class="text-end">Actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var vehicleClass in Model.VehicleClasses)
                                {
                                    <tr>
                                        <td>@vehicleClass.VehicleClassName</td>
                                        <td>
                                            <div class="d-flex justify-content-end gap-2">
                                                <a class="btn btn-sm btn-outline-primary"
                                                   asp-area="Admin"
                                                   asp-controller="VehicleClasses"
                                                   asp-action="Edit"
                                                   asp-route-id="@vehicleClass.VehicleClassId"
                                                   asp-route-returnUrl="@Model.ReturnUrl">Edit</a>
                                                @if (vehicleClass.CanDelete)
                                                {
                                                    <form method="post"
                                                          asp-area="Admin"
                                                          asp-controller="VehicleClasses"
                                                          asp-action="Delete"
                                                          asp-route-id="@vehicleClass.VehicleClassId"
                                                          asp-route-returnUrl="@Model.ReturnUrl">
                                                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                                    </form>
                                                }
                                                else
                                                {
                                                    <button type="button" class="btn btn-sm btn-outline-secondary" disabled>In Use</button>
                                                }
                                            </div>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
